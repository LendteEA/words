<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/wordsFragment">

    <fragment
        android:id="@+id/wordsFragment"
        android:name="com.bx.words.WordsFragment"
        android:label="Words"
        tools:layout="@layout/fragment_words">
        <action
            android:id="@+id/action_wordsFragment_to_wordsAddFragment"
            app:destination="@id/wordsAddFragment" />
    </fragment>
    <fragment
        android:id="@+id/wordsAddFragment"
        android:name="com.bx.words.WordsAddFragment"
        android:label="Add"
        tools:layout="@layout/fragment_words_add" />
</navigation>